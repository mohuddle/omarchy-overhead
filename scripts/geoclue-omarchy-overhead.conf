# Drop-in for /etc/geoclue/conf.d/ so Allow does not need a GNOME agent.
# Installed by: sudo cp scripts/geoclue-omarchy-overhead.conf /etc/geoclue/conf.d/

[omarchy-overhead]
allowed=true
system=false
users=
